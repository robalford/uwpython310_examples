# This file is required to create a Python package in a directory. The code in this file gets run when a package
# or any of its modules is imported. But it's often left empty.

# You can import an entire package with 'import my_python_package'. But you have to explicitly import each of its
# modules to bring them into the current namespace with 'from my_python_package import import_me'.

print("Run the __init__.py file on import")
